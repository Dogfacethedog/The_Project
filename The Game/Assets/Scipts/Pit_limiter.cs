using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class Pit_limiter : MonoBehaviour
{

    public Rigidbody rb;
    public float maxspeed = 200f;
    public bool capenabled = false;


    void OnCollisionEnter(Collision collisioninfo)
    {

        if (collisioninfo.collider = true)
        {

            cappedVelocity();

        }



    }



    public void cappedVelocity()
    {

        float capXvel = Mathf.Min(rb.velocity.x, maxspeed);

        float capYvel = rb.velocity.y;

        float capZvel = rb.velocity.z;

        rb.velocity = new Vector3(capXvel, capYvel, capZvel);

    }

}
*/